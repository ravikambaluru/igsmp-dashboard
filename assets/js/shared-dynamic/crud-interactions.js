let editHandler = (formRef, targetUrl, fetchUrl) => {

	// get the target form reference 

	// then get acess form elements refs from list 

	// perform backend API fetch and get details of particular elements with fetchAPI

	// then populate form elements fields with data recieved from backend

	// change form input title to update & add update button with update action  handler

	// udpate form action value to target value


	let formObject = document.getElementById(formRef).elements;

	let formArray = Object.entries(formObject);

	fetch(uuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuuui)


}
